'use client';
import { useState } from 'react';
export default function OrderForm({pageId,heading,button}){const [f,setF]=useState({name:'',phone:'',address:'',quantity:1}),[done,setDone]=useState(false),[msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();setMsg('Sending…');try{const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,pageId})}),d=await r.json();if(!r.ok)throw Error(d.error);setDone(true);setMsg('');setF({name:'',phone:'',address:'',quantity:1})}catch(e){setMsg(e.message||'Could not send order')}};if(done)return <div className="order-thankyou"><div className="thank-icon">✓</div><h3>Thank You! 🎉</h3><p>Your order has been received successfully.</p><small>We will contact you soon to confirm your order.</small><button type="button" onClick={()=>setDone(false)}>Place Another Order</button></div>;return <form className="order-form published-order" onSubmit={submit}><h3>{heading||'Order Now'}</h3><input required placeholder="Your Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input required placeholder="Phone Number" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/><textarea required placeholder="Delivery Address" value={f.address} onChange={e=>setF({...f,address:e.target.value})}/><input type="number" min="1" placeholder="Quantity" value={f.quantity} onChange={e=>setF({...f,quantity:e.target.value})}/><button type="submit">{button||'Place Order'}</button>{msg&&<small>{msg}</small>}</form>}
