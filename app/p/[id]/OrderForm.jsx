'use client';
import { useState } from 'react';
export default function OrderForm({pageId,heading,button,price}){const [f,setF]=useState({name:'',phone:'',address:'',quantity:1}),[done,setDone]=useState(false),[msg,setMsg]=useState('');const regular=Number(price?.regular||0),offer=Number(price?.offer||0),unit=offer||regular,total=unit*Number(f.quantity||1);const submit=async e=>{e.preventDefault();setMsg('Sending…');try{const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,pageId,quantity:Number(f.quantity||1),currency:price?.currency||'৳',regularPrice:regular,offerPrice:unit,total})}),d=await r.json();if(!r.ok)throw Error(d.error);setDone(true);setMsg('');setF({name:'',phone:'',address:'',quantity:1})}catch(e){setMsg(e.message||'Could not send order')}};if(done)return <div className="order-thankyou"><div className="thank-icon">✓</div><h3>Thank You! 🎉</h3><p>Your order has been received successfully.</p><small>We will contact you soon to confirm your order.</small><button type="button" onClick={()=>setDone(false)}>Place Another Order</button></div>;return <form className="order-form published-order" onSubmit={submit}><h3>{heading||'Order Now'}</h3>{unit>0&&<div className="order-price">{regular>unit&&<del>{price?.currency||'৳'}{regular}</del>} <strong>{price?.currency||'৳'}{unit}</strong>{regular>unit&&<span>{Math.round((regular-unit)/regular*100)}% OFF</span>}<small>Total: {price?.currency||'৳'}{total}</small></div>}<input required placeholder="Your Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input required placeholder="Phone Number" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/><textarea required placeholder="Delivery Address" value={f.address} onChange={e=>setF({...f,address:e.target.value})}/><input type="number" min="1" placeholder="Quantity" value={f.quantity} onChange={e=>setF({...f,quantity:e.target.value})}/><div className="order-total"><b>Total: {price?.currency||'৳'}{total}</b></div><button type="submit">{button||'Place Order'}</button>{msg&&<small>{msg}</small>}</form>}
